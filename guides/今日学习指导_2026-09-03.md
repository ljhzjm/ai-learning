# 今日学习指导 · 2026-09-03(第 1 周·周日,收官日)

> **任务**:综合练习 —— 待办事项 API(CRUD + 内存存储 + pytest 单测)
> **验收**:提交 GitHub,README 写清如何运行(预计 4h)

今天是第 1 周收官战:前 6 天学的 uv、FastAPI、Pydantic、Depends、异常处理、lifespan 全部用上,新朋友只有一个 —— **pytest**。做完今天,你就有了第一个「带测试、带 .env、可运行」的完整 FastAPI 项目(第 1 周周验收标准)。

## 🎯 先懂 3 个新概念(5 分钟)

| 概念 | 一句话解释 |
|---|---|
| **pytest** | Python 最主流的测试框架:测试就是**普通函数**,名字 `test_` 开头,里面写 `assert` 断言,`uv run pytest` 自动发现并全跑一遍 |
| **TestClient** | FastAPI 自带的"假浏览器":`TestClient(app)` 不用真起服务器,直接 `client.get("/todos")` 测接口,拿到的 Response 和 httpx 一样 |
| **fixture 夹具** | 每个测试前自动做的准备:`autouse=True` 的 fixture 会在**每个测试函数运行前**执行一次 —— 今天用它重置内存库 |

共同思想:**测试 = 把人肉点浏览器验证固化成代码**。以后每次改代码跑一遍 `uv run pytest`,全绿就是没改坏。今天起,验收从「我访问了一遍」升级为「测试全绿」。

## 🛠️ 第 1 步:建 w1d7 项目(10 分钟)

```powershell
cd D:\ITstudy\ai-learning
uv init practice/w1d7-todo-api
cd practice/w1d7-todo-api
uv add fastapi uvicorn pydantic-settings pytest httpx
```

> httpx 是给 TestClient 用的(新版 Starlette 的 TestClient 内部就是 httpx);pydantic-settings 照旧管 .env,周验收要求"带 .env"。

## 📦 第 2 步:配置 + 数据模型 + 内存存储(25 分钟)

**① `src\w1d7_todo_api\config.py`**(照搬前几天的套路;同时建 `.env.example` 写 `TODO_APP_NAME=待办事项 API`):

```python
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    app_name: str = "待办事项 API"
    model_config = {"env_file": ".env", "env_prefix": "TODO_"}

settings = Settings()
```

**② `src\w1d7_todo_api\schemas.py`** —— 请求/响应模型分开:

```python
from pydantic import BaseModel, Field

class TodoIn(BaseModel):
    title: str = Field(min_length=1, max_length=100)
    done: bool = False          # 建待办时可以不传,默认 False

class TodoOut(TodoIn):
    id: int                     # 输出多一个 id,由服务器分配
```

**③ `src\w1d7_todo_api\db.py`** —— 内存库 + 自定义异常 + 重置函数:

```python
class TodoNotFoundError(Exception):
    def __init__(self, todo_id: int):
        self.todo_id = todo_id
        super().__init__(f"待办 {todo_id} 不存在")

fake_db: dict[str, list[dict]] = {"todos": []}

def reset_db():
    """清空并预置一条数据。lifespan 启动时和测试夹具里都调它。"""
    fake_db["todos"] = [{"id": 1, "title": "完成第 1 周综合练习", "done": False}]
```

> 要点:`reset_db()` 单独抽出来,是因为**测试和启动要用同一份初始数据** —— 这正是第 1 周反复出现的"复用"思想。

## ⚙️ 第 3 步:main.py —— CRUD 五个接口(50 分钟)

```python
from contextlib import asynccontextmanager

from fastapi import Depends, FastAPI, Request
from fastapi.responses import JSONResponse

from w1d7_todo_api.config import settings
from w1d7_todo_api.db import TodoNotFoundError, fake_db, reset_db
from w1d7_todo_api.schemas import TodoIn, TodoOut

@asynccontextmanager
async def lifespan(app: FastAPI):
    reset_db()                            # 启动时预置数据
    print(f"[lifespan] {settings.app_name} 已启动")
    yield

app = FastAPI(title=settings.app_name, lifespan=lifespan)

def get_db():
    return fake_db

@app.exception_handler(TodoNotFoundError)
async def todo_not_found_handler(request: Request, exc: TodoNotFoundError):
    return JSONResponse(status_code=404, content={"error": "not_found", "message": str(exc)})

# C:创建
@app.post("/todos", response_model=TodoOut, status_code=201)
def create_todo(todo: TodoIn, db=Depends(get_db)):
    new_id = max((t["id"] for t in db["todos"]), default=0) + 1
    record = todo.model_dump()
    record["id"] = new_id
    db["todos"].append(record)
    return record

# R:查列表 + 查单条
@app.get("/todos", response_model=list[TodoOut])
def list_todos(db=Depends(get_db)):
    return db["todos"]

@app.get("/todos/{todo_id}", response_model=TodoOut)
def get_todo(todo_id: int, db=Depends(get_db)):
    for t in db["todos"]:
        if t["id"] == todo_id:
            return t
    raise TodoNotFoundError(todo_id)

# U:局部更新
@app.patch("/todos/{todo_id}", response_model=TodoOut)
def update_todo(todo_id: int, todo: TodoIn, db=Depends(get_db)):
    for t in db["todos"]:
        if t["id"] == todo_id:
            t.update(todo.model_dump(exclude_unset=True))   # 只更新请求里出现的字段
            return t
    raise TodoNotFoundError(todo_id)

# D:删除
@app.delete("/todos/{todo_id}", status_code=204)
def delete_todo(todo_id: int, db=Depends(get_db)):
    for i, t in enumerate(db["todos"]):
        if t["id"] == todo_id:
            db["todos"].pop(i)
            return                       # 返回 None,204 无响应体
    raise TodoNotFoundError(todo_id)
```

**验证**(起服务后逐个试):

```powershell
uv run uvicorn w1d7_todo_api.main:app --reload
# 浏览器开 http://127.0.0.1:8000/docs 点着玩一遍
curl.exe -X POST http://127.0.0.1:8000/todos -H "Content-Type: application/json" -d "{\"title\":\"学 pytest\"}"
curl.exe http://127.0.0.1:8000/todos
curl.exe -X PATCH http://127.0.0.1:8000/todos/1 -H "Content-Type: application/json" -d "{\"done\":true}"
curl.exe -X DELETE http://127.0.0.1:8000/todos/2 -i
```

> 要点:① `PATCH` = 局部更新,`exclude_unset=True` 表示"请求里没写的字段不动"—— 传 `{"done":true}` 不会把 title 清掉;② `response_model=list[TodoOut]` 让 FastAPI 帮你过滤/序列化输出;③ DELETE 返回 `None` 才有干净的 204 空响应体。

## 🧪 第 4 步:第一个 pytest 测试(30 分钟)

新建 `tests\test_todos.py`:

```python
import pytest
from fastapi.testclient import TestClient

from w1d7_todo_api.db import reset_db
from w1d7_todo_api.main import app

client = TestClient(app)        # 不用起服务器

@pytest.fixture(autouse=True)
def clean_db():
    reset_db()                  # 每个测试前重置,互不污染

def test_list_todos():
    r = client.get("/todos")
    assert r.status_code == 200
    todos = r.json()
    assert len(todos) == 1
    assert todos[0]["title"] == "完成第 1 周综合练习"

def test_create_todo():
    r = client.post("/todos", json={"title": "学 pytest"})
    assert r.status_code == 201
    body = r.json()
    assert body["id"] == 2          # 预置 1 条,新的是 2
    assert body["done"] is False    # 没传 done → 默认 False
```

跑: `uv run pytest` → `2 passed`。

> 要点:① 测试名字 `test_` 开头 pytest 才会发现;② `autouse=True` 的 fixture **每个测试前自动执行**,所以第二个测试里 id 才会是 2(库被重置过了);③ 断言失败时 pytest 会把两边值打出来,这就是调试线索。

## 🏁 第 5 步:验收挑战 —— 独立补全测试(60 分钟)

**不看上面的代码**,在 `tests\test_todos.py` 里独立补全以下测试(至少 6 个):

1. `test_get_todo` — 查 id=1 → 200 且 title 正确;查 id=999 → **404** 且 `error == "not_found"`
2. `test_update_todo_partial` — PATCH 只传 `{"done": true}` → 200,**title 保持不变**、done 变 true
3. `test_update_todo_missing` — PATCH 不存在的 id → 404
4. `test_delete_todo` — DELETE id=1 → **204**;再 GET /todos/1 → 404(真删掉了)
5. `test_delete_todo_missing` — DELETE 不存在的 id → 404
6. `test_create_validation` — POST `{"title": ""}` → **422**;POST title 101 个字 → 422(Field 校验在拦)

全部跑绿(`uv run pytest`,≥8 passed)= 今天「CRUD + pytest 单测」验收达成 ✅。加分项:`uv run pytest -v` 观察每个测试的名字与结果;自己再写一个中间件计时测试(响应头 `X-Process-Time`)。

> 提示:422 的响应体里 `detail` 字段能看到 Pydantic 的校验错误明细。

## 📖 第 6 步:README + 提交 GitHub(25 分钟)

写 `README.md`(验收要求:写清如何运行):

```markdown
# w1d7-todo-api

第 1 周综合练习:待办事项 API —— CRUD + 内存存储 + pytest 单测。

## 如何运行
uv run uvicorn w1d7_todo_api.main:app --reload
# 浏览器打开 http://127.0.0.1:8000/docs

## 如何测试
uv run pytest
```

> 写 README 是以后每个项目的固定收尾:别人(和一个月后的你)clone 下来,照着两条命令就能跑起来。

## ✅ 完成后

1. 在任务文件 `tasks/今日任务_2026-09-03.md` 里把 `- [ ]` 打勾成 `- [x]`(今天 watch 没开,打勾后跟 Claude 说「打勾了」,或自己跑 `python study.py sync`)
2. 写 5 行学习笔记:`python study.py note`
3. w1d7 项目 + README 要喊 Claude 手动 add + push(study.py publish 只推 tasks/ 和 notes/)
4. 卡壳超过 30 分钟,随时问 Claude

> 🎉 今天做完,第 1 周 7 天全部通关,周验收达成。明天(2026-09-04)进入 **第 2 周·周一:SQL 复习**(JOIN、GROUP BY、子查询、EXPLAIN)。
