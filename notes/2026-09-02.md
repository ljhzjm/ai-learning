# 学习笔记 · 2026-09-02 周三(计划进度:第 1 周·周六)

## 今日 5 行速记

1. **学了**:FastAPI 进阶(Depends 依赖注入/异常处理/中间件/lifespan)+ asyncio + httpx 异步并发(一天双杀两个计划日:周五 + 周六)
2. **核心坑**:① 裸标量参数 `discount: float` 被 FastAPI 当查询参数,请求体 422 —— 请求体必须用 Pydantic 模型;② Steam++ 加速器把 GitHub 域名劫持到 127.0.0.1,报 CERTIFICATE_VERIFY_FAILED
3. **硬核解法**:① `DiscountIn(BaseModel)` + `Field(ge=0, le=1)`;② 关掉加速器 hosts 即恢复;③ `asyncio.gather(..., return_exceptions=True)` 让单个请求失败不炸整批
4. **顺手解决**:GitHub API 匿名限速 60 次/小时,403 是配额不是 bug,`/rate_limit` 接口可查重置时间
5. **明天**:周日综合练习 —— 待办事项 API(CRUD + 内存存储 + pytest 单测)

---

## 今日完成事项

| 任务 | 结果 |
|---|---|
| FastAPI 进阶(第 1 周·周五) | ✅ 已完成(w1d5 重构,offer 接口已修) |
| asyncio + httpx 并发(第 1 周·周六) | ✅ 验收通过(compare 提速 10x,challenge 提速 3.2x;403 为限速,重置后复跑确认 200) |

## 未完成(顺延)

- 无 —— 任务 2 已验收通过,任务文件里还差手动打勾(watch 会自动同步)

---

## 常用命令速查(今天实际用过的)

- `uv run uvicorn w1d5_fastapi_refactor.main:app --reload` — 跑 w1d5 服务
- `uv run python src/w1d6_async_fetch/challenge.py` — 并发抓取 10 个 URL 验收脚本(提速 3x+)
- `python study.py next` — 把下一个计划日追加进今日清单
- 排查"证书验证失败":`python -c "import socket; print(socket.gethostbyname_ex('api.github.com'))"` 看域名是不是被解析成 127.0.0.1 → `netstat -ano | grep :443` 找监听进程 → `curl.exe --resolve api.github.com:443:140.82.112.6 https://api.github.com/zen` 直连真实 IP 验证
- `curl -s https://api.github.com/rate_limit` — 查 GitHub API 匿名限速剩余与重置时间

---

## 明天计划

- 工程底座(FastAPI + 异步 + 测试) · 周日:综合练习——待办事项 API(CRUD + 内存存储 + pytest 单测)
- 验收:提交 GitHub,README 写清如何运行(4h)

---

## 每周铁律

1. 周日综合练习提交 GitHub
2. 每天 5 行学习笔记(本文件顶部「今日 5 行速记」)
3. 卡壳 30 分钟就问 AI
