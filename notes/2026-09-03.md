# 学习笔记 · 2026-09-03 周四(计划进度:第 1 周·周日)

## 今日 5 行速记

1. **学了**:第 1 周综合练习 —— 待办事项 API(CRUD + 内存存储 + pytest 单测),**第 1 周 7/7 全部通关 🎉**
2. **核心坑**:Windows curl 传中文 JSON 报 `There was an error parsing the body` —— 是 curl 的编码问题,不是应用 bug(英文重测即过)
3. **硬核解法**:`pytest.fixture(autouse=True)` 每个测试前自动重置内存库,测试互不污染;验证接口用英文数据或 /docs 页面
4. **顺手解决**:pytest 9/9 全绿 + 真实服务冒烟通过 + w1d7 已推 GitHub(feat 44ddee4)
5. **明天**:第 2 周·周一 SQL 复习(JOIN、GROUP BY、子查询、EXPLAIN 看懂执行计划)

---

## 今日完成事项

| 任务 | 结果 |
|---|---|
| 综合练习(第 1 周·周日) | ✅ 已完成 —— w1d7-todo-api 推送 GitHub,验收 9/9 测试全绿 |

**第 1 周周验收达标**:能独立搭一个「带测试、带 .env、可运行」的 FastAPI 项目 ✅

---

## 常用命令速查(今天实际用过的)

- `uv run pytest -q` — 跑测试(今天 9 passed)
- `uv run uvicorn w1d7_todo_api.main:app --reload` — 起 w1d7 服务(冒烟用 `--port 8123` 避免冲突)
- `curl -s -X POST http://127.0.0.1:8123/todos -H "Content-Type: application/json" -d "{\"title\":\"smoke test\"}"` — 冒烟测试(**注意:Windows curl 传中文会报解析错,用英文**)
- `python study.py sync` — 同步手打的勾(规范化任务文件)
- `python study.py publish` — 推送 tasks/ 和 notes/ 到 GitHub
- `git add practice/<项目> && git commit -m "feat: ..." && git push` — 项目代码要**手动 add**(publish 不推 practice/)

---

## 明天计划

- 数据层(SQL → PostgreSQL → SQLAlchemy → Redis → Docker) · 周一:SQL 复习——JOIN、GROUP BY、子查询、EXPLAIN 看懂执行计划
- 验收:会手写 5 类常用查询(2h)
- 第 2 周大图:周一 SQL → 周二 Docker PG → 周三 SQLAlchemy 改造待办 API → 周四 Alembic → 周五 Redis → 周六 Dockerfile → 周日 docker-compose 综合练习

---

## 每周铁律

1. 周日综合练习提交 GitHub
2. 每天 5 行学习笔记(本文件顶部「今日 5 行速记」)
3. 卡壳 30 分钟就问 AI
