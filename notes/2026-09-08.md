# 学习笔记 · 2026-09-08 周二(计划进度:第 2 周·周三)

## 今日 5 行速记

1. **学了**:SQLAlchemy 2.0 —— ORM 模型声明(Mapped/mapped_column)、engine + session 工作单元、relationship 关系映射;待办 API 从内存 dict 改成 PG 落库,9 个测试全过,重启 API 数据还在
2. **核心坑**:① 返回 ORM 对象没开 `from_attributes=True` → 响应验证 500;② `GET /todos` 的 done 过滤无条件 `where(Todo.done == None)` → 生成 `IS NULL`,列表全空(测试抓到)
3. **硬核解法**:① `TodoOut` 加 `model_config = ConfigDict(from_attributes=True)`;② 过滤条件拼接:`done is not None` 时才 `stmt.where(...)`;③ SERIAL 序列坑:seed 显式插 id 后序列停在 1,插新行报 duplicate key → `setval(pg_get_serial_sequence('表','列'), MAX(id))` 修复
4. **顺手解决**:重建 pg-school 容器(昨天加分项删了)+ createdb 独立库 todo;测试夹具 `TRUNCATE ... RESTART IDENTITY`(清数据 + 序列归零);school_demo 演示了 lazy load 的 SQL(echo 开关)
5. **明天**:Alembic(首次迁移、加字段迁移、回滚)

---

## 今日完成事项

| 任务 | 结果 |
|---|---|
| SQLAlchemy 2.0(第 2 周·周三) | ✅ 已完成 |

验收记录:Claude 冒烟测试全部通过 —— pytest 9/9、curl CRUD + done 过滤 + 404、psql 直接查库确认落库、school_demo 平均分与周一 SQL 结果一致(张伟 88.33 / 李娜 84.0 / 刘洋 66.0)。

---

## 常用命令速查(今天实际用过的)

```powershell
# 重建容器(卷删了要重播种)+ 建独立库
docker run -d --name pg-school -p 5432:5432 -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=school -v pg-school-data:/var/lib/postgresql/data postgres:16-alpine
Get-Content schema.sql | docker exec -i pg-school psql -U postgres -d school
docker exec pg-school createdb -U postgres todo

# 项目
uv add sqlalchemy "psycopg[binary]"
uv run uvicorn w2d3_sqlalchemy_todo.main:app --reload
uv run pytest      # 9 passed
curl.exe "http://127.0.0.1:8000/todos?done=true"    # 过滤接口

# 查库 / 修序列
docker exec -i pg-school psql -U postgres -d todo -c "SELECT * FROM todos;"
SELECT setval(pg_get_serial_sequence('students','id'), (SELECT MAX(id) FROM students));

# 跨零点补收前一天
python study.py finish --draft 2026-09-08
```

---

## 明天计划

- 数据层(SQL → PostgreSQL → SQLAlchemy → Redis → Docker) · 周四:Alembic——首次迁移、加字段迁移、回滚
- 验收:迁移命令跑通并理解版本文件(2h)

---

## 每周铁律

1. 周日综合练习提交 GitHub
2. 每天 5 行学习笔记(本文件顶部「今日 5 行速记」)
3. 卡壳 30 分钟就问 AI
