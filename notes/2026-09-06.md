# 学习笔记 · 2026-09-06 周日(计划进度:第 2 周·周一)

## 今日 5 行速记

1. **学了**:SQL 复习(SQLite 零安装)—— 学生选课 3 表(students/courses/enrollments),手写 5 类查询:基础 WHERE/ORDER BY/LIMIT、聚合 GROUP BY/HAVING、JOIN(INNER/LEFT)、子查询(IN + 派生表)、EXPLAIN 执行计划
2. **核心坑**:① Windows GBK 控制台 `print` ✅/❌ emoji → UnicodeEncodeError(终端编码问题,不是代码错,VS Code 终端没事);② LEFT JOIN 反查「没选 XX 的」时,过滤条件必须写进 **ON** 而不是 WHERE,写进 WHERE 会把没匹配的行(刚补的 NULL 行)又滤掉
3. **硬核解法**:① 打印标记统一用 ASCII:`[OK]`/`[ERR]`;② 同类坑三连:curl 传中文 JSON 报错(w1d7)、GBK print emoji(今天)、乱码排查先看终端编码
4. **顺手解决**:challenge.py 5 题输出与预期全一致(88.33 / 84.0 / 82.0、SCAN→SEARCH USING INDEX);README 3 行补齐
5. **明天**:Docker 起 PostgreSQL,psql 常用命令、建表、索引,把今天 5 类查询在 PG 原样重跑对比()

---

## 今日完成事项

| 任务 | 结果 |
|---|---|
| SQL 复习(第 2 周·周一) | ✅ 已完成 |

---

## 常用命令速查(今天实际用过的)

```
uv init practice/w2d1-sql-basics                        # 建项目(src 布局)
uv run python src/w2d1_sql_basics/setup.py              # 建库播种(重复跑会重建)
uv run python src/w2d1_sql_basics/repl.py               # SQL 练习台(输一行查一行)
uv run python src/w2d1_sql_basics/challenge.py          # 5 道验收题
EXPLAIN QUERY PLAN SELECT * FROM enrollments WHERE course_id = 2   # SCAN → 建索引后 SEARCH
CREATE INDEX idx_enr_course ON enrollments(course_id)   # 建索引
```

---

## 明天计划

- 数据层(SQL → PostgreSQL → SQLAlchemy → Redis → Docker) · 周二:Docker 起 PostgreSQL,psql 常用命令、建表、索引——
- 验收:容器里建出带索引的表(2h)

---

## 每周铁律

1. 周日综合练习提交 GitHub
2. 每天 5 行学习笔记(本文件顶部「今日 5 行速记」)
3. 卡壳 30 分钟就问 AI
